#include "moviesADT.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int main(int count, char *doc[]){
    //chequeo que la cantidad de datos ingresada sea la correcta
    if (count != 8)
        {
            fprintf(stderr, "Cantidad incorrecta de datos\n");
            exit(1);
        }
    //
    
}
